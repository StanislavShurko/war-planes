export type WarPlane = {
  id: number;
  name: string;
  manufacturer: string;
  origin: string;
  role: string;
  firstFlight: number;
  topSpeedKmh: number;
  rangeKm: number;
  image: string; // placeholder URLs (swap with real images later)
};

export {};
